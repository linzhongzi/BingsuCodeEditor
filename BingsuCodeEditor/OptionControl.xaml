<UserControl x:Class="BingsuCodeEditor.OptionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BingsuCodeEditor" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Background="{DynamicResource MaterialDesignPaper}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
            Height="*" />
            <RowDefinition
            Height="Auto" />
        </Grid.RowDefinitions>
        <DockPanel>
            <TextBlock Text="基本设置" DockPanel.Dock="Top" Margin="10,10,10,10" FontSize="20"/>
            <UniformGrid Margin="5,5,5,5" HorizontalAlignment="Left" DockPanel.Dock="Top" VerticalAlignment="Top" Columns="2">
                <TextBlock Text="字体大小" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="CBFontSize" Width="100" materialDesign:TextFieldAssist.SuffixText=" P" HorizontalAlignment="Left" VerticalAlignment="Center">
                </ComboBox>
                <TextBlock Text="Tab大小" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="CBTabSize" Width="100" HorizontalAlignment="Left" VerticalAlignment="Center">
                </ComboBox>
                <Border/>
                <CheckBox x:Name="cbShowlineNumber" Content="显示行号" Style="{StaticResource MaterialDesignCheckBox}" />
                <Border/>
                <CheckBox x:Name="cbConvertTapToSpace" Content="使用空格代替Tab" Style="{StaticResource MaterialDesignCheckBox}" />
            </UniformGrid>
            <TextBlock Text="高亮颜色" DockPanel.Dock="Top" Margin="10,10,10,10" FontSize="20"/>
            <WrapPanel x:Name="ColorPallet" DockPanel.Dock="Bottom" Width="525" HorizontalAlignment="Left" VerticalAlignment="Stretch">
                
            </WrapPanel>
        </DockPanel>
        
        <materialDesign:ColorZone Mode="PrimaryDark" Grid.Row="1" Effect="{DynamicResource MaterialDesignShadowDepth5}">
            <DockPanel LastChildFill="False" Cursor="Hand">
                <TextBlock Margin="16" Text="文本编辑器选项"/>
                <StackPanel DockPanel.Dock="Right" Cursor="Hand" Orientation="Horizontal">
                    <Button Content="重置" Click="Reset_Click" Margin="5,5,15,5" />
                    <Button Content="保存" Click="Save_Click" Margin="5" Cursor="Hand"/>
                    <Button Content="取消" Click="Cancel_Click" Margin="5"/>
                </StackPanel>
             </DockPanel>
        </materialDesign:ColorZone>
    </Grid>
</UserControl>
